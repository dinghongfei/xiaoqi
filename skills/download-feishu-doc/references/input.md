# 输入

飞书文档 URL 或 token：

```
--url 'https://xxx.feishu.cn/docx/TOKEN'
--url 'https://xxx.feishu.cn/wiki/TOKEN' --section blog
```

工作区 `.env` 需有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` / `LARK_CLI_PROFILE`。本机已安装 `lark-cli`，命令会带 `--profile`。
