# 输入

工作区（`--root` 或 `BOT_ROOT` 或当前目录）：

```
preview/_wechat/          # 公众号预览页
data/jobs/                # 单次任务中间文件
data/last-job.json        # 上次任务索引
```

路径可由 `.env` 的 `HUGO_DEPLOY_DIR`、`JOBS_DIR`、`LAST_JOB_PATH` 覆盖。
